
networks_runsave.py:
  runs and saves datafiles

networks_project.py:
  run cell to obtain plots and statistics. Requires datafiles
  to have been created and stored from networks_runsave.py

EVERYTHING ELSE
  acts behind the scenes
